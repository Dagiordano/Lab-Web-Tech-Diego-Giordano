<h1><%= @post.title %></h1>

<p><%= @post.user.name %></p>


<p><%= @post.content %></p>

<p>
    <% @post.tags.each do |tag| %>
    <%= tag.name %>
  <% end %>


<h2>Comments</h2>
<%= render @post.comments%>

    
</p><h2>Add a comment:</h2>
<%= render 'comments/form'%>


<button type="button" class="btn btn-outline-info"><%= link_to "Edit", edit_post_path(@post) %></button>

<button type="button" class="btn btn-outline-secondary">  <%= link_to "Back to Posts", posts_path%>   </button>